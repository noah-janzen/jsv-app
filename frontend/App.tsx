import React from 'react';
import Navigator from './routes/rootTabNavigator'

export default function App() {
  // Return root tab navigator that contains all further screen of the app.
  return (<Navigator />);
}
