import * as React from 'react';
import { Text, View } from 'react-native';

export function ChatOverviewScreen() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Chat!</Text>
        </View>
    );
}